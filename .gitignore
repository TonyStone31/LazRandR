# build output
lib/
lazrandr
*.compiled
*.o
*.or
*.ppu
published/

# Lazarus session state (per-user window positions, not source)
*.lps

# scripts the app generates at runtime
xrandr_apply.sh
SETMYMONITORS.sh

# Lazarus IDE auto-backups
backup/
